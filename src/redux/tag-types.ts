export enum tagTypes {
  user = "User",
  admin = "Admin",
  member = "Member",
  flat = "flat",
  flatRequest = "flatRequest",
}
export const tagTypeList = [tagTypes.user, tagTypes.admin, tagTypes.member, tagTypes.flat, tagTypes.flatRequest];
